# Recorded venue data is large, irreplaceable, and never committed (BLUEPRINT §3).
data/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pycache__/
*.pyc

# The host repository's root .gitignore excludes CLAUDE.md everywhere, as a
# stray agent-tool file. Here it is the project constitution that BLUEPRINT §3
# requires at the project root, so it is re-included.
!CLAUDE.md
